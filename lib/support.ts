/**
 * Support contact details.
 *
 * One constant, because this address appears in the footer, on all three legal
 * pages, in the contact page copy and in the Organization schema — and a
 * structured-data contactPoint that disagrees with the address on the page is
 * a trust signal pointing the wrong way.
 */
export const SUPPORT_EMAIL = 'support@usecrawlable.com';
